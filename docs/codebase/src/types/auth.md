# types/auth.ts

**File:** `src/types/auth.ts`  
**Lines:** 29  
**Status:** Active

## Purpose

Authentication and page access control types.

## Exports

- `AuthGuardProps` — `{ children, fallback? }`
- `LoginPromptProps` — `{ message?, onLogin? }`
- `ComingSoonProps` — `{ title?, description?, expectedDate?, showBackButton? }`
- `PageAccessProps` — `{ requiresAuth, comingSoon? }`
