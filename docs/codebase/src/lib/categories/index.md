# categories/index.ts

**File:** `src/lib/categories/index.ts`  
**Lines:** 36  
**Status:** Active

## Purpose

Central barrel export for the categories module. Re-exports `CategoriesService`, `CategoriesRepository`, all types, utility functions, and constants.

## Exports

- `CategoriesService` (class)
- `CategoriesRepository` (class)
- 11 type exports (Category, Subcategory, DTOs, result types, query options)
- 4 utility functions (`validateName`, `sortByOrderAndName`, `isHebrewText`, etc.)
- Constants (`COLLECTIONS`, `DEFAULTS`, `ERROR_MESSAGES`, `SUCCESS_MESSAGES`)
