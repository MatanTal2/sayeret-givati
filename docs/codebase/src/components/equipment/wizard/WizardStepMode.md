# WizardStepMode

**File:** `src/components/equipment/wizard/WizardStepMode.tsx`

Step 1: pick `single` or `bulk`. Two card buttons; bulk shows a hint reminding the user that each item still needs its own S/N and photo. No async work; pure state toggle.
