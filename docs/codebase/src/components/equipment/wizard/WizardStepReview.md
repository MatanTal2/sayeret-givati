# WizardStepReview

**File:** `src/components/equipment/wizard/WizardStepReview.tsx`

Step 4: read-only summary before submit. Lists the picked template plus each item with photo thumb + S/N + catalog + location. No state — purely a render of the in-progress wizard state for one last sanity check before the batch write.
