# AuthModal.tsx

**File:** `src/components/auth/AuthModal.tsx`
**Lines:** 17
**Status:** Active

## Purpose

Thin public API wrapper around `AuthModalController`. Fixes `initialModal="login"` and exposes a cleaner prop interface. This is the component that external callers (like `GlobalAuthModal`) use.
